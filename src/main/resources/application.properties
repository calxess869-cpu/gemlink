# 1. Temporarily skip the database auto-configuration check so it boots up cleanly
spring.autoconfigure.exclude=org.springframework.boot.autoconfigure.jdbc.DataSourceAutoConfiguration

# 2. Tell the embedded Tomcat web server to bind dynamically to Railway's assigned web port
server.port=${PORT:8080}
